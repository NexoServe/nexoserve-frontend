import useStyles from './css';

const OrderNavbarBottom = () => {
  const classes = useStyles();

  return (
    <div className={classes.orderNavbarBottom}>
      <div>Bottom</div>
    </div>
  );
};

export default OrderNavbarBottom;
