query ValidateShoppingCart($order: CreateOrderInput!) {
  validateShoppingCart(order: $order) {
    subTotal
    tax
    tip
    grandTotal
    orderItems {
      id
      food {
        id
        name
        price
      }
      foodSize {
        id
        name
        price
      }
      items {
        id
        name
        price
        addOnName
        itemSize {
          id
          name
          price
        }
      }
      total
      quantity
    }
  }
}
