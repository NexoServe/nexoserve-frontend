query ValidateShoppingCart($order: CreateOrderInput!) {
  validateShoppingCart(order: $order) {
    subTotal
    tax
    tip
    grandTotal
    orderItems {
      id
      food {
        id
        name
        price
      }
      foodSize {
        id
        name
        price
      }
      options {
        id
        name
        price
        addOnName
        optionSize {
          id
          name
          price
        }
      }
      total
      quantity
    }
  }
}
