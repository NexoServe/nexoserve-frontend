fragment AddOnFields on AddOnType {
  id
  name
  isRequired
  maxOptionsSelected
  options {
    id
    name
    price
    default
    sort
    optionSizes {
      id
      name
      price
      default
      sort
    }
  }
}
