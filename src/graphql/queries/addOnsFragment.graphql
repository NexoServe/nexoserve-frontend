fragment AddOnFields on AddOnType {
  id
  name
  isRequired
  maxOptionsSelected
  type
  options {
    id
    name
    price
    default
    sort
    optionSizes {
      id
      name
      price
      default
      sort
    }
  }
}
