mutation CheckoutCalculateMut(
  $input: [ShoppingCartInput]!
  $paymentMethodId: String!
) {
  CheckoutCalculateMut(input: $input, paymentMethodId: $paymentMethodId) {
    id
    total
    clientSecret
    status
  }
}
